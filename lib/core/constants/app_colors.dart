import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primary = Color(0xFF4A90E2);
  static const Color secondary = Color(0xFF50C878);
  static const Color background = Color(0xFFFFF9F0);

  // Text colors
  static const Color textPrimary = Color(0xFF000000);
  static const Color textSecondary = Color(0xFF666666);
  static const Color textHint = Color(0xFF999999);

  // UI colors
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);
  static const Color grey = Color(0xFFE0E0E0);
  static const Color greyLight = Color(0xFFF5F5F5);

  // Gradient colors
  static const Color gradientStart = Color(0xFF4A90E2);
  static const Color gradientEnd = Color(0xFF50C878);

  // Welcome screen colors
  static const Color welcomeBlue = Color(0xFF2563EB);
  static const Color welcomeBlueDark = Color(0xFF1E40AF);
}
